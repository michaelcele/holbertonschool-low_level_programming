#include "holberton.h"
/**
 * print_array  - Print every arrays
 *
 * @array: String
 * @n: integer
 *
 */
void print_array(int *array, int n)
{
	int j;

	for (j = 0; j < n; j++)
	{
		if (i > 0)
			printf(", ");
		printf("%d", *(array + i));
	}										}
	printf("\n");
}
